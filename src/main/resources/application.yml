spring:
  application:
    name: document-post-processing-solution
  datasource:
    url: jdbc:h2:mem:test-db
    username: sa
    password:
    driver-class-name: org.h2.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
#    properties:
#      hibernate:
#        format_sql: true
  h2:
    console:
      path: /h2-console
      enabled: true

server:
  port: 8080